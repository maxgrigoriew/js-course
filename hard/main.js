"use strict"

const getRandomInt = function (min, max) {
    
}
const getRandomInt = function (n) {
    return !isNaN(parseInt(n) && isFinite(n))
}

cost gameBot = function (atЕempts, min, max) {
    attempts = attempts || 10;
    min = min || 0;
    max = max || 100;
    
}